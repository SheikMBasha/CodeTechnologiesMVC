@model CodeTechnologiesMVC.trainer

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>trainer</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.TrainerName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.TrainerName)
            @Html.ValidationMessageFor(model => model.TrainerName)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.TrainerPhone)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.TrainerPhone)
            @Html.ValidationMessageFor(model => model.TrainerPhone)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
